const footerStyles = {
  container: {
    padding: 2
  },
  iconContainer: {
    paddingX: 3
  },
  mediaIcon: theme => ({
    color: theme.palette.primary.contrastText
  })
};

export default footerStyles;
